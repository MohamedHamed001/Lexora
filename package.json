{
  "name": "lexora",
  "private": true,
  "type": "module",
  "scripts": {
    "build:firefox": "node scripts/build-firefox.mjs",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier -w .",
    "test": "vitest run --environment jsdom extension/tests",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "eslint": "^9.0.0",
    "jsdom": "^29.1.0",
    "playwright": "^1.55.0",
    "prettier": "^3.0.0",
    "vitest": "^4.1.5"
  }
}
